<style>
    .RewardsWord_boxLeftBoxTitle {
        margin: 0;
    }

    @media(max-width:999px) {
        #shopify-section-{{section.id}}.RewardsWord_all{
            margin-top: {{section.settings.Mobile_top}}px;
            margin-bottom: {{section.settings.Mobile_bottom}}px;

            padding-top:{{section.settings.Mobile_paddingTop}}px;
            padding-bottom:{{section.settings.Mobile_paddingBottom}}px;
        }
    }
    @media(min-width:1000px) {
        
        #shopify-section-{{section.id}}.RewardsWord_all{
            margin-top: {{section.settings.Desktop_top}}px;
            margin-bottom: {{section.settings.Desktop_bottom}}px;

            padding-top:{{section.settings.Desktop_paddingTop}}px; 
            padding-bottom:{{section.settings.Desktop_paddingBottom}}px;
        }


    }
    @media(min-width:1000px) {   
        .RewardsWord_boxLeftBoxTitle {
            text-align: center;
        }
        .RewardsWord_bottom {
            margin-top: 48px;
            display: flex;
            gap: 64px;
            justify-content: center;
        }
        .RewardsWord_bottomBox {
            text-align: center;
        }
        .RewardsWord_bottomBox_title {
            font-weight: 600;
            margin-top: 16px;
        }
        .RewardsWord_bottomBox_text {
            margin-top: 4px;
        }
    }
    @media(max-width:999px) {   
        .RewardsWord_boxLeftBoxTitle {
            text-align: center;
        }
        .RewardsWord_bottom {
            margin-top: 24px;
            display: grid;
            gap: 32px;
        }
        .RewardsWord_bottomBox {
            text-align: center;
        }
        .RewardsWord_bottomBox_title {
            font-weight: 600;
            margin-top: 16px;
        }
        .RewardsWord_bottomBox_text {
            margin-top: 4px;
        }
    }
</style>
<div class="RewardsWord_all" id="shopify-section-{{section.id}}"> 
    <div class="my_container">  
        <div class="RewardsWord_box">
            {% if section.settings.title !=blank %}
                <h2 class="RewardsWord_boxLeftBoxTitle my_h_32 my_m_28">
                    {{section.settings.title}}
                </h2>
            {% endif %}
            <div class="RewardsWord_bottom">
                {% for block in section.blocks %}
                    <div class="RewardsWord_bottomBox">
                        {% if block.settings.svg !=blank %}
                            {{block.settings.svg}}
                        {% endif %}
                        {% if block.settings.title !=blank %}
                        <div class="RewardsWord_bottomBox_title my_h_20 my_m_20">
                            {{block.settings.title}}
                        </div>
                        {% endif %}
                        {% if block.settings.text !=blank %}
                            <div class="RewardsWord_bottomBox_text my_h_14 my_m_14">
                                {{block.settings.text}}
                            </div>
                        {% endif %}
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>  
</div>

{% schema %}
  {
    "name": "Rewards Word",
    "class": "RewardsWord",
    "settings":[
       
        {
            "type": "textarea",
            "id": "title",
            "label": "title"
        },
        {
            "type": "header",
            "content": "Spacing Top:"
        },
        {
            "type": "number",
            "id": "Desktop_top",
            "label": "Desktop Margin Top",
            "default": 0
        },
        {
            "type": "number",
            "id": "Mobile_top",
            "label": "Mobile Margin Top",
            "default": 0
        },
        {
            "type": "number",
            "id": "Desktop_paddingTop",
            "label": "Desktop Padding Top",
            "default": 0
        },
        {
            "type": "number",
            "id": "Mobile_paddingTop",
            "label": "Mobile Padding Top",
            "default": 0
        },
        
        {
            "type": "header",
            "content": "Spacing Bottom:"
        },
        {
            "type": "number",
            "id": "Desktop_bottom",
            "label": "Desktop Margin Bottom",
            "default": 0
        },
        {
            "type": "number",
            "id": "Mobile_bottom",
            "label": "Mobile Margin Bottom",
            "default": 0
        },
        {
          "type": "number",
          "id": "Desktop_paddingBottom",
          "label": "Desktop Padding Bottom",
          "default": 0
        },
        {
            "type": "number",
            "id": "Mobile_paddingBottom",
            "label": "Mobile Padding Bottom",
            "default": 0
        }
    ],
"blocks":[
    {
        "type": "text",
        "name": "text",
        "settings": [
           
            {
                "type": "textarea",
                "id": "svg",
                "label": "svg"
            },
            {
                "type": "textarea",
                "id": "title",
                "label": "title"
            },
            {
                "type": "textarea",
                "id": "text",
                "label": "text"
            }
        ]
    }
    ],
    "presets": [
        {
            "name": "Rewards Word"
        }
    ]
  }
  
  
{% endschema %}